export default function checkResolve() {
  return new Promise((resolve, reject) => {
    // resolve({ gender: "male" });
    // console.log('checkResolve ...............');
    reject(new Error("/dashboard"));
  });
}
